export default function HomePage() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>City Data Analyzer</h1>
      <p>
        This is a placeholder Next.js frontend workspace. Build your UI here while the
        Python API backend lives under <code>apps/python-backend</code>.
      </p>
    </main>
  );
}
